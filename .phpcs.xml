<?xml version="1.0"?>
<ruleset name="Godyar CMS">
  <description>Project coding standard for GitHub/Codacy scanning.</description>

  <rule ref="PSR12"/>

  <!-- Exclude non-core / template output files to avoid noisy false positives -->
  <exclude-pattern>admin/*</exclude-pattern>
  <exclude-pattern>frontend/views/*</exclude-pattern>
  <exclude-pattern>frontend/templates/*</exclude-pattern>
  <exclude-pattern>public/*</exclude-pattern>
  <exclude-pattern>seo/*</exclude-pattern>
  <exclude-pattern>oauth/*</exclude-pattern>
  <exclude-pattern>plugins/*</exclude-pattern>
  <exclude-pattern>tools/*</exclude-pattern>
  <exclude-pattern>webroot/*</exclude-pattern>
  <exclude-pattern>uploads/*</exclude-pattern>
  <exclude-pattern>cache/*</exclude-pattern>
  <exclude-pattern>storage/*</exclude-pattern>

</ruleset>
